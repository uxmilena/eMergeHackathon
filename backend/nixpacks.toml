# When Railway "Root Directory" is `backend`, Nixpacks would otherwise run `python main.py`,
# which exits immediately (no server). Bind to 0.0.0.0 and Railway's PORT.

[start]
cmd = "/opt/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
